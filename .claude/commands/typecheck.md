---
description: Type-check .ts and .vue files without building
---

Run `npm run check-typescript-errors-vue` (vue-tsc, covers .vue SFCs; superset of plain tsc).

Report only: pass/fail, and if it fails, the list of errors with file:line. Do not restate the command output verbatim.
