---
description: Lint the project with autofix
---

Run `npm run lint`.

Warnings are expected and normal in this repo (pre-existing baseline). Only report errors that are new or block the current change — don't list every pre-existing warning.
