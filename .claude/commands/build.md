---
description: Type-check and build the production bundle
---

Run `npm run build` (runs `vue-tsc --noEmit` then `vite build` into `dist/`).

Report only: pass/fail, and if it fails, the first error with its file:line. Do not summarize successful output.
