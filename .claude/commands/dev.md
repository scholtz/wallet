---
description: Start the Vite dev server in the background
---

Start `npm run serve` in the background (it runs on http://localhost:8080 with HMR and does not exit). Do not wait for it to "finish" — confirm it's listening (watch for the Vite ready message) and then return control.
